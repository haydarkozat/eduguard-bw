# Copy to .env and adjust. Never commit the real .env.
POSTGRES_USER=eduguard
POSTGRES_PASSWORD=eduguard_dev_pw
POSTGRES_DB=eduguard
OLLAMA_MODEL=llama3
EMBED_MODEL=nomic-embed-text
# Set to 1 if the host can't grant CAP_NET_RAW (Scapy falls back to mock sweeps).
EDUGUARD_SIMULATE=0
